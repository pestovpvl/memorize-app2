<%= form_with(model: leitner_card_box) do |form| %>
  <% if leitner_card_box.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(leitner_card_box.errors.count, "error") %> prohibited this leitner_card_box from being saved:</h2>

      <ul>
        <% leitner_card_box.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :repeat_period, style: "display: block" %>
    <%= form.number_field :repeat_period %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
